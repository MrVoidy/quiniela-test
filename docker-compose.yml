services:
  postgres-db:
    image: postgres:16-alpine
    container_name: quiniela_postgres
    environment:
      POSTGRES_USER: quiniela
      POSTGRES_PASSWORD: quiniela
      POSTGRES_DB: quiniela_db
    ports:
      - "5432:5432"
